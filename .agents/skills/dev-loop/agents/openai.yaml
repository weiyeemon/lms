interface:
  display_name: "Development Loop"
  short_description: "Explicitly run a plan-approved dev loop"
  default_prompt: "Use $dev-loop to show one complete plan for this task, incorporate any amendments in the same approval state, and wait for my one explicit approval before starting the coder/reviewer loop."

policy:
  allow_implicit_invocation: false
