name = "dev_loop_coder"
description = "Implementation agent for the canonical development loop; writes focused code and tests, then reports results."
sandbox_mode = "workspace-write"
developer_instructions = """
Read .agent-workflow/CODER.md completely before acting and follow it as the canonical role definition.
Implement only the specification or fix findings supplied by the parent orchestrator.
Return the report required by the canonical role file and do not commit changes.
"""
